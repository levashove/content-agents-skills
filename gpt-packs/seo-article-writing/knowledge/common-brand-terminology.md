# Терминология и продуктовые названия

Обязательные правила именования. Канон типографики, феминитивов, списков и валюты — редстандарт вендора (навык `redstandard_skill` из «brand» (внутренний материал системы, в эту выгрузку не входит), инъецируется автору и редактору); здесь — машинный минимум, который стерегут детекторы, и generic-механика.

<!-- BEGIN generated: product-names — источник «» (внутренний материал системы, в эту выгрузку не входит) (поле naming:), генератор «generate_brand_terminology» (внутренняя автопроверка репозитория, в чате недоступна — проверь этот пункт вручную); правь скиллы, не таблицу -->
## Продуктовые названия VK Tech

| Правильно | Неправильно |
|-----------|-------------|
| VK Cloud | VKCloud, ВК Клауд, vk cloud |
| VK Tech | ВК Тех |
| Tarantool | Тарантул, tarantool (в начале предложения) |
| VK Data Platform | ВК Дата |
| VK WorkSpace | ВК Воркспейс |
| VK HR Tek | VK HR Tech, VK HRTek, ВК ХР Тек, HR-Tek |
| Object Storage (полное — VK Object Storage) | Cloud Storage, VK Объектное хранилище, Object Storage VK, VK Cloud Object Storage |
| Managed Kubernetes (полная форма — Managed Kubernetes от VK Cloud) | Managed Kubernetes VK Cloud, VK Cloud Managed Kubernetes, VK Managed Kubernetes |

> Каноническое имя S3-сервиса VK Cloud — **Object Storage** (полная вендорская форма — **VK Object Storage**). «Cloud Storage» как название продукта не используется. «Объектное хранилище» — только как тип/описание (строчными), не как имя продукта. **«VK Cloud Object Storage» — не имя продукта:** VK Cloud — платформа, в составе которой сервис поставляется, а не часть его названия; склейка платформы с продуктом даёт третью, несуществующую форму. Пишем «Object Storage» или «VK Object Storage», при необходимости уточняя платформу отдельно («Object Storage в VK Cloud»). Доменные дельты не переопределяют это имя.

> Kubernetes-сервис платформы называется **Managed Kubernetes**; прежнее имя **Cloud Containers** устарело и в новых материалах не используется (в документации и старых публикациях оно ещё встречается — при цитировании помечаем как прежнее название). Managed Kubernetes — внутриплатформенный продукт уровня 3: приставку `VK` он не носит и упоминается только в связке с платформой через предлог — **«Managed Kubernetes от VK Cloud»**. Формы «Managed Kubernetes VK Cloud», «VK Cloud Managed Kubernetes» и «VK Managed Kubernetes» — не имена продукта: склейка платформы с сервисом даёт третью, несуществующую форму (тот же дефект, что «VK Cloud Object Storage»). Допустимо уточнять платформу отдельно: «Managed Kubernetes в VK Cloud».
<!-- END generated: product-names -->

### SEO-ключи и регистр продуктовых имён (без keyword-stuffing)

SEO-ключи живут во `frontmatter` (`keywords:`). В тексте они **не вставляются дословно строчными ради плотности** — склоняются и капитализируются по русскому языку. Это рецидивный дефект: в статьях про GPU Operator и Argo CD ключи протекали в прозу как есть (`argo cd kubernetes помогает вынести…`, `gitops автоматизация не заменяет…`, `argo cd vk cloud`).

| Правильно | Неправильно (keyword-stuffing) |
|-----------|--------------------------------|
| Argo CD в Kubernetes помогает вынести релизы | argo cd kubernetes помогает вынести релизы |
| GitOps-автоматизация не заменяет дисциплину | gitops автоматизация не заменяет дисциплину |
| аддон Argo CD для VK Cloud | аддон argo cd vk cloud |
| откат в Kubernetes | откат деплоя kubernetes |

Правила:

- **Продукты и платформы — каноническая форма всегда:** **Argo CD, Kubernetes, GitOps, VK Cloud, VK Tech, Tarantool, OpenStack, ClickHouse, PostgreSQL, Greenplum** — не строчными в прозе. Строчная форма в адресе/домене (`kubernetes.io`), хештеге (`#kubernetes`) и подвальном блоке ключей (`**Primary**: …`) допустима — это не проза.
- **CLI-инструменты** (Helm, Kustomize, Docker, Terraform, Ansible, Prometheus, Grafana, Redis, Kafka, OpenSearch) в прозе тоже капитализируются, но строчная форма бывает уместной (имя бинаря/команды: `helm template`; сравнение «helm vs kustomize») — поэтому это мягкий сигнал на ручной просмотр, не жёсткий запрет.
- Предложение **не начинается** со строчного ключа или продукта.
- Многословный ключ не вставляется в текст дословно строчными — переформулируется по смыслу.
- **Ключ не выделяется `**жирным**` или `*курсивом*` в прозе ради SEO.** Выделение ключевой фразы (например, `**CedrusData Engine vs Trino**` в лиде) — набивка под поисковик, а не смысловой акцент; жирным выделяется значимый термин по делу, не повтор заголовочного ключа. Ключи живут во `frontmatter`, в подвальном блоке `**Primary**: …` и в самом заголовке — но не дублируются выделением в тексте.
- Автопроверка — «check_keyword_stuffing» (внутренняя автопроверка репозитория, в чате недоступна — проверь этот пункт вручную). Пять сигналов: `lowercase-product` (продукты/платформы строчными — `BLOCK`, блокирует CI на изменённом контенте через внутренний гейт репозитория (в чате недоступен)), `keyword-bold-stuffing` (SEO-ключ жирным в прозе — `BLOCK`, тот же гейт), `sentence-initial-key` (предложение начинается со строчного ключа — `BLOCK`, тот же гейт), `keyword-verbatim` (дословный многословный ключ строчными — `REVIEW`), `cli-tool-lowercase` (CLI-инструменты — `REVIEW`). Списки имён — `CANON_PRODUCTS` и `CLI_TOOLS` в скрипте, сверяются с «products» (внутренний материал системы, в эту выгрузку не входит); пополнять только после прогона по корпусу `library/` (новое имя не должно флагать существующий корректный контент).

## Домен вендора

В новых материалах адрес платформы — **cloud.vk.ru** (канон — «brand» (внутренний материал системы, в эту выгрузку не входит) → `docs.site_base`; решение владельца 2026-08-26: дока, прайс и выдача живут на .ru). `cloud.vk.com` допустим только внутри цитаты старой публикации; в собственном тексте, ссылках и сносках — .ru (ревью 2026-09-04, E-14: домен .com жил в 40 строках фактуры продуктов). Legacy-корпус на `.com` не мигрируется (решение 2026-09-07, ROADMAP R-008: редирект изнутри среды не проверить); ссылка на legacy-хост из `brand.yaml` → `legacy_blog_hosts` даёт advisory `LEGACY_DOMAIN_LINK`, счётчик по корпусу под baseline «не растёт». Вопрос домена закрыт: фактчекер и оркестратор **не выносят его в `open_items`** отчёта, новые ссылки просто ставятся на `.ru`.

## Общая терминология

| Правильно | Неправильно |
|-----------|-------------|
| ИТ | IT (в русском тексте) |
| ИИ | AI (в русском тексте; исключения — имена продуктов и фич латиницей: AI Ассистент, VK AI Space, AI Persona, а также устойчивые англ. термины из словаря redstandards вроде «AI-powered») |
| вы (с маленькой) | Вы |
| бэкап | бекап, backup (в русском тексте) |
| дата-центр | датацентр, ДЦ (при первом упоминании) |
| email | e-mail |
| SLA | — среднего рода (соглашение) |
| Big Data, Cloud Native, Open Source | с прописных |
| PostgreSQL | Postgres, постгрес |

## Типографика (машинный минимум)

Полный канон (числа, даты, время, единицы, валюта, Ё, ссылки, эмодзи, списки, подписи) — редстандарт → «Типографика». Здесь только то, что проверяет машина или что нужно стадии без редстандарта в контексте:

- Кавычки: «ёлочки», внутри „лапки“ (закрывающая — U+201C, не прямая `"`)
- Длинное тире (—): в предложениях, **норматив ≤ 2 на 1000 знаков** (детектор `dash-density` в «check_ai_patterns» (внутренняя автопроверка репозитория, в чате недоступна — проверь этот пункт вручную), `DASH_PER_1000_LIMIT`; превышение — почти всегда маркер генерации; тире по умолчанию заменяется запятой или двоеточием, см. `technical-punctuation.md`). Выше норматива допустим только **функциональный остаток** в плотной технической прозе — каждое тире несёт функцию и остаток пообъектно обоснован в отчёте редактора (`final-trust-editing.md` → «Плотность тире»); блокирующий потолок — 3,5/1000
- Короткое тире (–): в диапазонах (2020–2024)
- Числа: пробел в разрядах (10 000), проценты слитно (50%)
- Заголовки: обычный регистр (с заглавной только первое слово и имена собственные) — не «Title Case» и не КАПС; точка в конце не ставится (вопрос/восклицание — можно). Заголовок описателен (структура — `text-accessibility.md`)

## Unicode-гигиена

Не стиль, а **целостность символов**: текст должен состоять из правильных кодовых точек. Артефакты генерации и копипаста невидимы глазом, но ломают поиск, diff, якоря и SEO.

- **Невидимые символы** (zero-width: U+200B/200C/2060, soft hyphen U+00AD, LRM/RLM, BOM U+FEFF) — удалять всегда. Легитимного применения в русской прозе нет; частый след LLM-вывода и копипаста. **Блокирующий** сигнал. Эмодзи-склейка (ZWJ U+200D, VS-16 U+FE0F) — исключение, сохраняется.
- **Bidi-контролы** (U+202A–202E, U+2066–2069, U+061C) — невидимое управление направлением текста; в русской прозе не встречаются, известный вектор подмены отображения (Trojan Source). **Блокирующий** сигнал.
- **Tag characters** (U+E0001–E007F) — невидимый «дублирующий алфавит» ASCII, канал скрытого текста и водяных знаков в LLM-выводе (ASCII smuggling). **Блокирующий** сигнал.
- **Line/paragraph separators** (U+2028/2029) — невидимые разрывы строк, ломают CMS/JS-вёрстку; заменять на обычный перенос. **Блокирующий** сигнал.
- **Private Use Area** (U+E000–F8FF) — кодовые точки без стандартного значения: обёртки цитат при стриминге веб-поиска чат-бота (U+E200–E202), «квадратики» иконочных шрифтов, логотип платформы U+F8FF после копипаста. Человек их не печатает, в русской прозе легитимного применения нет; удалять, ссылку/цитату восстанавливать без обёртки. **Блокирующий** сигнал (`pua`).
- **Экзотические пробелы** (U+2000–200A, U+202F narrow NBSP — частый след ChatGPT-вывода, U+205F, U+1680, U+3000) — визуально неотличимы от пробела, почти всегда артефакт копипаста/LLM; заменять на обычный пробел или NBSP. Advisory: может быть осознанной тонкой типографикой. Обычный NBSP (U+00A0) легитимен и не флагается.
- **Гомоглифы** — латинская буква-двойник внутри кириллического слова (`Сloud`, `caталог`, `Атlassian`): выглядит идентично, но это другой символ. Приводить слово к одному алфавиту. Сигнал **advisory**: у смешанного скрипта есть легитимный хвост — бренды (`СберCloud`, `МWS`), транслит-склонения (`Robом`, `partам`), римские цифры (`АгроXXI`); решение принимает редактор.
- **Mojibake** — кириллица/пунктуация, перекодированная через CP1252 (`Ð¾Ð±Ð»Ð°ÐºÐ¾`, `â€¦`): перекодировать в UTF-8. Advisory.
- **NBSP** (неразрывный пробел) после одно-буквенных предлогов и между числом и единицей (`5 кг`, `в облаке`) — типографика, advisory.

Слой автопроверки — «check_unicode_hygiene» (внутренняя автопроверка репозитория, в чате недоступна — проверь этот пункт вручную) (внутренний гейт репозитория (в чате недоступен); блокирующий гейт zero-width/BOM/bidi/tag-chars/line-sep/PUA на изменённом контенте — внутренний гейт репозитория (в чате недоступен)). Идея — порт из внешнего скилла типографики. Файлы храним в UTF-8 без BOM (`safe-file-save.md`).

## Русификация внутренних терминов

В клиентских текстах (лендинги, рассылки, пресс-релизы) англоязычные внутренние термины заменяются русскими эквивалентами. В экспертных текстах (статьи на Хабр, техническая документация) допустимы оригиналы.

| Клиентский текст | Не использовать | Где допустимо |
|------------------|-----------------|---------------|
| команда архитекторов | Professional Services | экспертные статьи |
| «Магазин приложений» | маркетплейс (для раздела VK Cloud) | общеупотребительный контекст |
| оплата только за использованные ресурсы | pay-as-you-go | экспертные статьи, документация |

## Феминитивы

Новообразованные феминитивы запрещены (редакторка, директорка, программистка и т. п.); традиционные — «учительница», «официантка», «предпринимательница» — норма. Канон — редстандарт → «Феминитивы».
